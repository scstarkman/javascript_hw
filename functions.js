//function 1: divide two numbers
var divideMe = function(x,y) {
return x/y;
}
divideMe(45,5);
9

//function 2: function to add an element to an array.
function addToArray(x){
  return colors.push(x);
}

addToArray('yellow')

console.log(colors);
["blue", "orange", "pink", "green", "yellow"]


//function 3; function to set variables using 3 arguments
function greatMeal(a,b,c){
    var entree = (a);
    var dessert = (b);
    var drink = (c);
    var dinner = entree + dessert + drink;

    return dinner;
}

greatMeal('pasta with italian sausages,',' anything chocolate,',' chardonnay');
"pasta with italian sausages, anything chocolate, chardonnay"
